from setuptools import setup, find_packages

setup(
    name="llmops-workshop-document-portal",
    author="Chandan Y S",
    version="0.1",
    packages=find_packages(), # Whichever the folder has __init__.py, will be considered as a package
)